server:
  port: 8088
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
    register-with-eureka: true  
spring:
  application:
    name: enriquecedor
  cloud:
    config:
      fail-fast: false    
      discovery:
        enabled: true
        service-id: config-server
