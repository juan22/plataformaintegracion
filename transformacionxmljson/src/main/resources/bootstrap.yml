server:
  port: 8086
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
    register-with-eureka: true  
spring:
  application:
    name: transformacion2
  cloud:
    config:
      fail-fast: false    
      discovery:
        enabled: true
        service-id: config-server
